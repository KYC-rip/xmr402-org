# Manual publish — 2026-09-05

The scheduled run could not reach `xmr402.org` from the sandbox (DNS blocked: `EAI_AGAIN xmr402.org`).
Same failure as 2026-09-04, so **two posts are now pending**. Files are in the working tree.

Run from the project root on your machine:

```bash
npm run publish-post -- posts/reversal-paradox-chargeback-freeze-finality-xmr402.json   # still pending from 09-04
npm run publish-post -- posts/cloudflare-pay-handles-agent-naming-layer-xmr402.json     # today
npm run deploy   # needed so both cover SVGs in public/ are served
```

## Today's post

- **Slug:** `cloudflare-pay-handles-agent-naming-layer-xmr402`
- **Title (en):** The Name Is the Chokepoint: Why cloudflare.pay Gives Every Agent a Permanent Payment Address — and Why XMR402 Agents Stay Nameless
- **Angle:** Cloudflare's Aug 4, 2026 Wallets / `cloudflare.pay` launch. The spend caps got the headlines; the *naming layer* is the structural change. A resolvable payment handle is the join key that turns a transparent ledger into a named, attributable behavioural record — and every resolver is a place someone can say no. XMR402's one-time subaddress + per-request TX Proof leaves nothing to resolve or revoke.
- **Languages:** en, zh-TW, ru, es, pt, ja (~1,900 words en)
- **Diagrams:** 2 mermaid per language (handle-resolution flowchart with control points; sequence diagram comparing named-handle vs XMR402 flow)
- **Tables:** 2 per language (what each layer knows; cloudflare.pay vs XMR402 property comparison)
- **Cover:** `public/cloudflare-pay-handles-agent-naming-layer-xmr402-cover.svg`

## Uniqueness check

Zero duplicated paragraphs against the 17 existing posts. Highest token-level Jaccard overlap
is 0.20 (`reversal-paradox…`), consistent with shared vocabulary rather than shared content.
Closest thematic neighbours — `identity-trap-agentkit-mpp-privacy` (identity binding) and
`pay-per-crawl-training-data-trail-xmr402` (Cloudflare metering) — are deliberately steered
around: this post is about the *namespace and its resolver*, not identity passports or crawl billing.

## Editorial notes

- Cloudflare has not named a custodian, the supported stablecoins, or the settlement networks;
  the post states this rather than assuming. Funding/spending/merchant support are described as
  future tense, matching Cloudflare's own copy.
- The post explicitly credits bounded delegation as a real improvement, and concedes that named,
  auditable rails are correct for enterprise procurement. The argument is against attribution as
  the *default*, not against attribution.
