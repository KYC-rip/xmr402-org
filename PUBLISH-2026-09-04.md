# Manual publish — 2026-09-04

The scheduled run could not reach `xmr402.org` (sandbox DNS blocked: `EAI_AGAIN`).
Both files are committed to the working tree. To publish from your machine:

```bash
npm run publish-post -- posts/reversal-paradox-chargeback-freeze-finality-xmr402.json
npm run deploy   # needed so the cover SVG in public/ is served
```
